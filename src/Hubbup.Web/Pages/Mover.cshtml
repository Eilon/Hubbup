@page
@using Microsoft.AspNetCore.Authentication
@{
    ViewData["PageTitlePrefix"] = $"Issue Mover";
    ViewData["CurrentGroup"] = "Mover";

    var gitHubAccessToken = await HttpContext.GetTokenAsync("access_token");
}

@*<base href="/Mover/" />*@

<app>@(await Html.RenderComponentAsync<IssueMoverClient.Pages.IssueMover>())</app>

<script src="_framework/blazor.server.js"></script>

<script>

    window.GetGitHubAccessToken = function () {
        return @Json.Serialize(gitHubAccessToken);
    };

    window.updateTooltips = function (elem) {
        $(elem).tooltip();
    };
</script>
